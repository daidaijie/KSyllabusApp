package com.daijie.ksyllabusapp.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.daijie.ksyllabusapp.repository.lessonrule.LessonRuleRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "LESSON_RULE_RECORD".
*/
public class LessonRuleRecordDao extends AbstractDao<LessonRuleRecord, Long> {

    public static final String TABLENAME = "LESSON_RULE_RECORD";

    /**
     * Properties of entity LessonRuleRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property LessonRecordsJson = new Property(1, String.class, "lessonRecordsJson", false, "LESSON_RECORDS_JSON");
        public final static Property Account = new Property(2, String.class, "account", false, "ACCOUNT");
        public final static Property SemesterYear = new Property(3, int.class, "semesterYear", false, "SEMESTER_YEAR");
        public final static Property SemesterSeason = new Property(4, int.class, "semesterSeason", false, "SEMESTER_SEASON");
    }


    public LessonRuleRecordDao(DaoConfig config) {
        super(config);
    }
    
    public LessonRuleRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"LESSON_RULE_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"LESSON_RECORDS_JSON\" TEXT," + // 1: lessonRecordsJson
                "\"ACCOUNT\" TEXT," + // 2: account
                "\"SEMESTER_YEAR\" INTEGER NOT NULL ," + // 3: semesterYear
                "\"SEMESTER_SEASON\" INTEGER NOT NULL );"); // 4: semesterSeason
        // Add Indexes
        db.execSQL("CREATE INDEX " + constraint + "IDX_LESSON_RULE_RECORD_ACCOUNT ON \"LESSON_RULE_RECORD\"" +
                " (\"ACCOUNT\" ASC);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_LESSON_RULE_RECORD_SEMESTER_YEAR ON \"LESSON_RULE_RECORD\"" +
                " (\"SEMESTER_YEAR\" ASC);");
        db.execSQL("CREATE INDEX " + constraint + "IDX_LESSON_RULE_RECORD_SEMESTER_SEASON ON \"LESSON_RULE_RECORD\"" +
                " (\"SEMESTER_SEASON\" ASC);");
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"LESSON_RULE_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, LessonRuleRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lessonRecordsJson = entity.getLessonRecordsJson();
        if (lessonRecordsJson != null) {
            stmt.bindString(2, lessonRecordsJson);
        }
 
        String account = entity.getAccount();
        if (account != null) {
            stmt.bindString(3, account);
        }
        stmt.bindLong(4, entity.getSemesterYear());
        stmt.bindLong(5, entity.getSemesterSeason());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, LessonRuleRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String lessonRecordsJson = entity.getLessonRecordsJson();
        if (lessonRecordsJson != null) {
            stmt.bindString(2, lessonRecordsJson);
        }
 
        String account = entity.getAccount();
        if (account != null) {
            stmt.bindString(3, account);
        }
        stmt.bindLong(4, entity.getSemesterYear());
        stmt.bindLong(5, entity.getSemesterSeason());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public LessonRuleRecord readEntity(Cursor cursor, int offset) {
        LessonRuleRecord entity = new LessonRuleRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // lessonRecordsJson
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // account
            cursor.getInt(offset + 3), // semesterYear
            cursor.getInt(offset + 4) // semesterSeason
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, LessonRuleRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setLessonRecordsJson(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setAccount(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setSemesterYear(cursor.getInt(offset + 3));
        entity.setSemesterSeason(cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(LessonRuleRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(LessonRuleRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(LessonRuleRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
